# Creating bundle for the browser client

To bundle the library for the client, we use webpack. The bundles are generated in the `/dist` folder.

- To create development bundle: `$ npm run build:dev`
- To create production bundle: `$ npm run build:prod`
