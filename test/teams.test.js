import test from 'ava'
import Teams from '../src/teams'

test('Teams: get(role, callback)', async t => {
  t.fail('todo')
})

test('Teams: getProjects(owner, callback)', async t => {
  t.fail('todo')
})

test('Teams: addProject(owner, project, callback)', async t => {
  t.fail('todo')
})

test('Teams: getProject(owner, projectId, callback)', async t => {
  t.fail('todo')
})

test('Teams: updateProject(owner, projectId, project, callback)', async t => {
  t.fail('todo')
})

test('Teams: removeProject(owner, projectId, callback)', async t => {
  t.fail('todo')
})

test('Teams: getUserInfo(username, callback)', async t => {
  t.fail('todo')
})
